SELECT 
    MAIN_TB.NAME,
    MAIN_TB.COUNT
FROM 
    (SELECT
        NAME,
        COUNT(ANIMAL_ID) AS COUNT
    FROM
        ANIMAL_INS 
    WHERE 
        NAME IS NOT NULL
    GROUP BY NAME) MAIN_TB
WHERE
    MAIN_TB.COUNT >= 2
ORDER BY MAIN_TB.NAME
