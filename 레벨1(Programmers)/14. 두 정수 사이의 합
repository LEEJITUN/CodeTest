
class Solution {
    public long solution(int a, int b) {
        long answer = 0;
        
        return cal(Math.max(a,b), Math.min(a,b));
    }
    
    // 등차수열 : 항 * (a+b)/2
    private long cal(long a, long b){
        return (a - b +1) * (a + b)/2;
    }
}
